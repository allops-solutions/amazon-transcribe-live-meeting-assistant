#if ( $ctx.error )
  $util.error($ctx.error.message, $ctx.error.type)
#end

## allOps policy: every authenticated user (Admin or User) can list every
## virtual participant — see getCall.response.vtl for the rationale. No
## Owner/SharedWith filtering here by design.
$util.toJson($context.result.items)
