#if ( $ctx.error )
  $util.error($ctx.error.message, $ctx.error.type)
#end

## allOps policy: every authenticated user (Admin or User) can read every
## meeting — only the admin-only pages (User Management, MCP Servers, Nova
## Sonic, Transcript Summary) stay Admin-gated, at the route/UI layer and via
## the @aws_cognito_user_pools(cognito_groups: ["Admin"]) directives on those
## mutations. No Owner/SharedWith restriction here by design.
$utils.toJson($context.result)
